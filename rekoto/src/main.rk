import "./test.rk";

struct st {
  a: number,
  b: number,
}

fn main() {
  let a = test.test(1 + 1, 1) + 1;
  a++;
  print(a);

  let str = "Hello";
  print(str + "World");

  let b = st {
    a:1,
    b:1,
  }

  let c = b.a;
  print(c);
}